version: '3'

vars:
  NAMESPACE: inference-gateway
  RELEASE_NAME: inference-gateway
  CLUSTER_NAME: mcp-cluster
  # Envoy Gateway's chart installs the Gateway API CRDs it supports (v1.6).
  ENVOY_GATEWAY_VERSION: v1.9.1
  OPERATOR_VERSION: v0.25.1
  COMPOSE_MCP_DIR: ../../docker-compose/mcp

tasks:
  default:
    desc: 'Show available options for the MCP Kubernetes deployment'
    silent: true
    cmds:
      - echo "🚀 MCP Kubernetes Deployment (operator-based)"
      - echo ""
      - echo "🎯 Common tasks:"
      - echo "   task deploy     - Deploy everything (infra, MCP servers, gateway)"
      - echo "   task test       - Run MCP integration tests"
      - echo "   task clean      - Tear everything down"
      - echo ""
      - echo "📋 Run 'task --list' to see all available tasks"

  validate-requirements:
    desc: 'Validate that all required tools are installed'
    silent: true
    cmds:
      - |
        command -v ctlptl >/dev/null 2>&1 || { echo "❌ ctlptl is required"; exit 1; }
        command -v kubectl >/dev/null 2>&1 || { echo "❌ kubectl is required"; exit 1; }
        command -v helm >/dev/null 2>&1 || { echo "❌ helm is required"; exit 1; }
        command -v docker >/dev/null 2>&1 || { echo "❌ docker is required"; exit 1; }
        command -v curl >/dev/null 2>&1 || { echo "❌ curl is required"; exit 1; }
        command -v jq >/dev/null 2>&1 || { echo "❌ jq is required"; exit 1; }
        echo "✅ All required tools are installed!"

  deploy-infrastructure:
    desc: 'Deploy infrastructure (k3d cluster, Envoy Gateway with the Gateway API, operator)'
    deps:
      - validate-requirements
    cmds:
      - ctlptl apply -f Cluster.yaml
      - kubectl wait --for=condition=Ready nodes --all --timeout=300s
      - task: deploy-envoy-gateway
      - task: deploy-inference-gateway-operator
      - echo "🚀 Infrastructure deployed successfully!"

  deploy-envoy-gateway:
    desc: 'Deploy Envoy Gateway (with its Gateway API CRDs) and register the envoy GatewayClass'
    cmds:
      - |
        helm upgrade --install envoy-gateway \
          oci://docker.io/envoyproxy/gateway-helm \
          --version {{.ENVOY_GATEWAY_VERSION}} \
          --namespace envoy-gateway-system \
          --create-namespace \
          --wait
      - kubectl -n envoy-gateway-system wait --for=condition=Available --timeout=120s deployment/envoy-gateway
      - |
        kubectl apply -f - <<'EOF'
        apiVersion: gateway.networking.k8s.io/v1
        kind: GatewayClass
        metadata:
          name: envoy
        spec:
          controllerName: gateway.envoyproxy.io/gatewayclass-controller
        EOF
      - echo "🚪 Envoy Gateway deployed successfully!"

  deploy-inference-gateway-operator:
    desc: 'Deploy the Inference Gateway operator'
    cmds:
      - kubectl apply -f https://github.com/inference-gateway/operator/releases/download/{{.OPERATOR_VERSION}}/install.yaml
      - kubectl -n inference-gateway-system wait --for=condition=Available --timeout=120s deployment/operator-inference-gateway
      - echo "🛠️ Inference Gateway operator deployed successfully!"

  build-mcp-servers:
    desc: 'Build the MCP servers (time, search, filesystem, pizza, calculator) and import them into k3d'
    cmds:
      - echo "🔨 Building MCP servers..."
      # Tagged :dev, not :latest, so the MCP resources' default pull policy uses
      # the imported images instead of trying a registry.
      - docker build -t mcp-time-server:dev time-server
      - docker build -t mcp-search-server:dev search-server
      - docker build -t mcp-filesystem-server:dev filesystem-server
      # The official-SDK servers are shared with the docker-compose example.
      - docker build -t mcp-pizza-server:dev {{.COMPOSE_MCP_DIR}}/pizza-server
      - docker build -t mcp-calculator-server:dev {{.COMPOSE_MCP_DIR}}/calculator-server
      - |
        k3d image import \
          mcp-time-server:dev \
          mcp-search-server:dev \
          mcp-filesystem-server:dev \
          mcp-pizza-server:dev \
          mcp-calculator-server:dev \
          --cluster {{.CLUSTER_NAME}}
      - echo "🔧 MCP servers built and imported successfully!"

  deploy-mcp-servers:
    desc: 'Deploy the MCP servers (as MCP resources) and the MCP Inspector'
    cmds:
      - task: build-mcp-servers
      - kubectl apply -f mcp-servers.yaml
      - kubectl apply -f inspector.yaml
      - echo "🔧 MCP servers and inspector deployed successfully!"

  deploy-inference-gateway:
    desc: 'Deploy the Inference Gateway with MCP configuration via the operator'
    cmds:
      - kubectl apply -f gateway.yaml
      - kubectl -n {{.NAMESPACE}} wait --for=create deployment/{{.RELEASE_NAME}} --timeout=60s
      - kubectl -n {{.NAMESPACE}} rollout status deployment/{{.RELEASE_NAME}} --timeout=180s
      - echo "🚪 Inference Gateway with MCP deployed successfully!"
      - echo "ℹ️  Add your provider API keys to the inference-gateway-secrets Secret in gateway.yaml."

  deploy:
    desc: 'Deploy the complete MCP setup'
    cmds:
      - task: deploy-infrastructure
      - task: deploy-mcp-servers
      - task: wait
      - task: deploy-inference-gateway
      - echo "🎉 Complete MCP setup deployed successfully!"

  wait:
    desc: 'Wait for the MCP servers to be ready'
    cmds:
      - kubectl wait --for=jsonpath='{.status.ready}'=true mcp --all -n {{.NAMESPACE}} --timeout=300s
      - echo "✅ MCP servers are ready!"

  test:
    desc: 'Run MCP integration tests (health, the gateway as an MCP server)'
    deps:
      - test:health
      - test:mcp-endpoint
    cmds:
      - echo "🎉 All MCP integration tests passed successfully!"

  test:health:
    desc: 'Test the gateway health endpoint in-cluster'
    cmds:
      - kubectl delete pod test-pod-01 --ignore-not-found=true -n {{.NAMESPACE}}
      - |
        kubectl run test-pod-01 --rm -i --restart=Never --image=curlimages/curl -n {{.NAMESPACE}} -- \
          curl -f http://{{.RELEASE_NAME}}:8080/health
      - echo "✅ Gateway is healthy"

  test:mcp-endpoint:
    desc: 'Test the gateway as an MCP server (POST /mcp, MCP 2026-07-28) in-cluster'
    cmds:
      - |
        kubectl run test-pod-02 --rm -i --restart=Never --image=curlimages/curl -n {{.NAMESPACE}} -- \
          curl -sf -X POST http://{{.RELEASE_NAME}}:8080/mcp \
            -H 'Content-Type: application/json' \
            -H 'MCP-Protocol-Version: 2026-07-28' \
            -H 'Mcp-Method: tools/list' \
            -d '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{"_meta":{"io.modelcontextprotocol/protocolVersion":"2026-07-28","io.modelcontextprotocol/clientInfo":{"name":"test","version":"1.0"},"io.modelcontextprotocol/clientCapabilities":{}}}}'
      - echo "✅ The gateway answers as an MCP server"

  port-forward:
    desc: 'Forward the MCP Inspector UI (6274) and its MCP Apps sandbox (6275) to localhost'
    cmds:
      - 'echo "MCP Inspector: open the tokenized URL from task inspector-url (Ctrl+C to stop)"'
      - kubectl port-forward -n {{.NAMESPACE}} service/mcp-inspector 6274:6274 6275:6275

  inspector-url:
    desc: 'Print the MCP Inspector URL, which carries its session token'
    cmds:
      - kubectl logs -n {{.NAMESPACE}} deployment/mcp-inspector | grep -o 'http://localhost:6274?MCP_INSPECTOR_API_TOKEN=[^ ]*' | tail -1

  port-forward-gateway:
    desc: 'Forward the Envoy data plane for the gateway to http://localhost:8080'
    cmds:
      - |
        ENVOY_SVC=$(kubectl get svc -n envoy-gateway-system \
          -l gateway.envoyproxy.io/owning-gateway-name=inference-gateway \
          -o jsonpath='{.items[0].metadata.name}')
        echo "Gateway: http://localhost:8080 (Host: api.inference-gateway.local)"
        kubectl -n envoy-gateway-system port-forward "svc/${ENVOY_SVC}" 8080:80

  logs:gateway:
    desc: 'Show logs from the inference gateway'
    cmds:
      - kubectl logs -l app={{.RELEASE_NAME}} -n {{.NAMESPACE}} --tail=100 -f

  restart:
    desc: 'Restart the gateway (e.g. after editing secrets)'
    cmds:
      - kubectl rollout restart deployment/{{.RELEASE_NAME}} -n {{.NAMESPACE}}
      - kubectl rollout status deployment/{{.RELEASE_NAME}} -n {{.NAMESPACE}}

  undeploy:
    desc: 'Remove the MCP components'
    cmds:
      - kubectl delete -f gateway.yaml --ignore-not-found=true
      - kubectl delete -f inspector.yaml --ignore-not-found=true
      - kubectl delete -f mcp-servers.yaml --ignore-not-found=true

  clean:
    desc: 'Clean up everything including the cluster'
    cmds:
      - ctlptl delete -f Cluster.yaml
      - echo "🧹 Everything cleaned up successfully!"
