---
version: '3'

vars:
  GATEWAY_API_VERSION: v1.5.1
  ENVOY_GATEWAY_VERSION: v1.2.0
  OPERATOR_VERSION: v0.16.3

tasks:
  deploy-infrastructure:
    desc: 'Deploy hybrid infrastructure'
    cmds:
      - task: create-cluster
      - task: deploy-gateway-api
      - task: deploy-envoy-gateway
      - task: deploy-inference-gateway-operator
      - echo "🚀 Hybrid infrastructure deployed successfully!"

  create-cluster:
    desc: 'Create a Kubernetes cluster using ctlptl'
    cmds:
      - ctlptl apply -f Cluster.yaml
      - echo "🛠️ Cluster created successfully!"

  deploy-gateway-api:
    desc: 'Install the Kubernetes Gateway API CRDs'
    cmds:
      - kubectl apply -f https://github.com/kubernetes-sigs/gateway-api/releases/download/{{.GATEWAY_API_VERSION}}/standard-install.yaml
      - echo "🌐 Gateway API CRDs installed successfully!"

  deploy-envoy-gateway:
    desc: 'Deploy Envoy Gateway and register the envoy GatewayClass'
    cmds:
      - |
        helm upgrade --install envoy-gateway \
          oci://docker.io/envoyproxy/gateway-helm \
          --version {{.ENVOY_GATEWAY_VERSION}} \
          --namespace envoy-gateway-system \
          --create-namespace \
          --skip-crds \
          --wait
      - kubectl -n envoy-gateway-system wait --for=condition=Available --timeout=120s deployment/envoy-gateway
      - |
        kubectl apply -f - <<'EOF'
        apiVersion: gateway.networking.k8s.io/v1
        kind: GatewayClass
        metadata:
          name: envoy
        spec:
          controllerName: gateway.envoyproxy.io/gatewayclass-controller
        EOF
      - echo "🚪 Envoy Gateway deployed successfully!"

  deploy-inference-gateway-operator:
    desc: 'Deploy the Inference Gateway operator'
    cmds:
      - kubectl apply -f https://github.com/inference-gateway/operator/releases/download/{{.OPERATOR_VERSION}}/install.yaml
      - kubectl -n inference-gateway-system wait --for=condition=Available --timeout=120s deployment/operator-inference-gateway
      - echo "🛠️ Inference Gateway operator deployed successfully!"

  deploy-ollama:
    desc: 'Deploy deepseek-r1:1.5b with Ollama'
    cmds:
      - kubectl create namespace ollama -o yaml --dry-run=client | kubectl apply -f -
      - kubectl apply -f ollama/
      - kubectl -n ollama rollout status deployment ollama
      - echo "🤖 Ollama with deepseek model deployed successfully!"

  watch-ollama-download:
    desc: 'Watch download progress'
    cmds:
      - kubectl -n ollama logs deployments/ollama -c ollama-model-puller -f
      - echo "👀 Watching Ollama model download progress..."

  deploy-llamacpp:
    desc: 'Deploy an optional local llama.cpp server (downloads a GGUF model on first start)'
    cmds:
      - kubectl apply -f llamacpp/
      - echo "🦙 llama.cpp server deploying - the model downloads on first start."
      - echo "   Watch it with: kubectl -n llamacpp logs -f deploy/llamacpp"

  deploy-inference-gateway:
    desc: 'Deploy the Inference Gateway with hybrid providers via the operator'
    cmds:
      - kubectl apply -f gateway.yaml
      - echo "🚪 Inference Gateway with hybrid providers deployed successfully!"

  clean:
    desc: 'Clean up the cluster'
    cmds:
      - ctlptl delete -f Cluster.yaml
      - echo "🧹 Cluster cleaned up successfully!"
